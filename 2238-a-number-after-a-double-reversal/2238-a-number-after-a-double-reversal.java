class Solution {
    public boolean isSameAfterReversals(int num) {
        int count=0,rem;
        if(num==0)
        return true;
        rem=num%10;
        if(rem==0)
        return false;
        return true;
        
    }
}